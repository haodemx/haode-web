# 项目 B 集成进度

## 2026-09-24 收尾复核

- 正确仓库再次确认：`haodemx/haode-web`；本轮从已包含 PR #91 的 `origin/main` 建立隔离分支，没有重复移植旧任务。
- 146 个 feed 候选与两份 2026-09-24 工作簿逐项映射，156 个工作簿产品行中 10 行明确排除；客户版与老板版销售层级 146/146 一致。
- 官网 / App / 结构化数据各有 73/146 与客户版一致；70 项属于新价格来源版本差异但缺少公开档位批准，2 项仅 VIP 档不同且不得导出，另有 1 项官网/App/结构化数据彼此数值不一致；34 项品质文字需人工确认。差异已按产品 ID 和档位记录，没有自动改价。
- 132 条现有图片路径、14 条物理缺图再次确认；5 项匹配审批清单，6 项明确 QC 失败并从 feed 图片候选中拒绝，10 项因跨品质或系列共图需人工复核。Feed 现保留 126 条图片链接，图片阻塞共 20 项。
- Feed 继续保持 `price=null`、`availability=unknown`、`platform_ready=false`；历史库存快照未作为当前库存。
- 本轮只新增脱敏审计、报告和回归测试；未上传 Feed、未发布广告、未改生产数据、未部署。
- 当前交付分支：`fix/growth-feed-closeout-20260924`；已审计实现提交：`f1a5055b5c0b0b4b83965b0a0a95a8945d0da0d7`；Draft PR #92。后续仅补交本证据文档，并由 PR HEAD CI 重新验证。
- exact head 的 push run `36071149616` 与 pull_request run `36071196216` 均在 Ubuntu 24.04 / Node 24.21.0 / Chromium 148 环境实际完成 336/336 浏览器测试；两轮 `haode-check`、`critical-business-gate` 与 `verify-transferred-artifact` 全部通过。
- 提交前本地完整浏览器最后一轮为 335/336，失败的未修改 Z Fold6 布局测量随后单跑通过；保留为根因未确认的本地环境/并行负载敏感记录，不把单跑记为本地全量通过，也未弱化断言或增加超时。

## 历史集成背景（PR #91 前置阶段，非当前分支/证据）

- 任务：Issue #43 官网候选 Feed、广告准备与转化追踪。
- 正确仓库：`haodemx/haode-web`。
- 当前基线：`origin/main` 的 `54d7e29e9aae9a00896148a8bfa854ecb0a06827`。
- 集成分支：`fix/growth-repair-issue-43-20260924`。
- 来源提交：`79c47a88451d99488a3ffddd15d45eff1a1c7e8e`；仅移植该任务提交，没有合并 PR #90 的旧历史。
- 边界：不合并 `main`、不部署、不上传 Feed、不创建广告账户、不付费、不发布广告、不写生产业务数据。

### 历史已完成

- 保留当前 `main` 的价格、WhatsApp、SEO、隐私、页面结构和受控公开包规则，完成冲突集成。
- 生成 146 条候选：141 条屏幕、4 条 Hydrogel、1 条 X200T；官网源有 132 条现有主图路径和 14 条物理缺图，收尾 QC 后 feed 拒绝 6 条错误主图并保留 126 条图片链接。
- 全部候选保持 `price=null`、`availability=unknown`、`platform_ready=false`；没有用 `0` 或猜测值填补缺失数据。
- 本地转化仅实现 `ViewProduct`、`WhatsAppClick`、已确认且非重放的 `Lead`；`Purchase` 与外部传输均未配置。
- 同意、撤回、去重、敏感字段过滤、商品归属、晚加载事件、失败登记与发布包依赖均有回归覆盖。
- `npm run build` 通过；受控公开包 1197 个文件，完整性校验通过。
- 专项单元 16/16、专项浏览器 18/18、完整浏览器 336/336 通过。

### 历史保留阻塞

- 0 条候选可直接上传：仍缺真实获批价格、库存/可售状态、品牌映射、14 张确认图片，以及平台账户、市场、格式与素材审批。
- 没有可信付款完成信号，因此不得产生 `Purchase`、收入或 ROAS。
- 生产发布、Feed 上传、广告发布和付费均未执行。

### 历史证据说明

`public-source-audit.json`、`local-qa.json` 和 `verification-manifest.json` 是来源任务在 PR #90 工作树生成的只读快照；当前集成以本文件及 `VERIFICATION.md` 的当前 `main` 验证为准。来源快照没有被伪装为当前 CI 或生产验证。
